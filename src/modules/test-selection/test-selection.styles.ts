import { makeStyles } from '@material-ui/core/styles';

export const useTestsStyles = makeStyles({
    container: {
        display: 'flex',
        flexDirection: 'column',
        maxWidth: 512,
        padding: 18,
        boxSizing: 'border-box',
        margin: '0 auto',
        marginTop: 32,
    },
});
